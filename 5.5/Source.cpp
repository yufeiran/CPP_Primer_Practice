#include<iostream> 
#include<string>

using namespace std;

int main()
{
	//string nowStr, lastStr;
	//bool sameFlag = false;
	//while (cin >> nowStr) {
	//	if (nowStr == lastStr) {
	//		if (isupper(nowStr[0])) {
	//			sameFlag = true;
	//			break;
	//		}

	//	}

	//	lastStr = nowStr;
	//}
	//if (sameFlag) {
	//	cout << "The same str is " << nowStr << endl;
	//}
	//else {
	//	cout << "End of strings" << endl;
	//}
	int sz;
	do {
		cin >> sz;

	}while(sz <= 0);

	return 0;
}